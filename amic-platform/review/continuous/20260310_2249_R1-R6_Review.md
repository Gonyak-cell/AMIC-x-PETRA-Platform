# Continuous Review — R1-R6 (13개 관점 통합)

> **Date**: 2026-03-10 22:49 (KST)
> **Round**: R1-R6 (전체)
> **Scope**: 마케팅 스테이지 → 딜 파이프라인 자동 동기화 (커밋 99af62d 기반, 7개 파일 +250/-46)
> **Protocol**: VCP Lite (Read→Grep→Self-Challenge SC-1/3/5)
> **Agents**: 4개 병렬 (R1, R2+R4, R3+R5, R6)

## 변경 파일 요약

```
deal-mgmt/app/models/enums.py              |   2 +
deal-mgmt/app/routers/buyer_marketing.py    | 100 +++++++++++++
deal-mgmt/migrations/versions/078_...py     |  20 +++
amic-platform/src/modules/ma/constants/buyer.ts | 30 +++-
amic-platform/src/modules/ma/types/marketing_log.ts | 2 +
amic-platform/src/modules/ma/hooks/useMarketingLogs.ts | 9 ++
amic-platform/src/modules/ma/components/buyers/MarketingTimelineView.tsx | 4 +-
```

---

## 발견 이슈

### P1 — 스프린트 우선 (점수: 60-89)

#### [R3-C1] DELETE 감사 로그 Enum 직렬화 리스크 — [Critical/MEDIUM] — Priority: P1 (60)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py`
- **카테고리**: R3 데이터 정합성
- **검증**: Read(V) Grep(V) SC-1(V) SC-3(△) SC-5(V)
- **이슈**: `delete_marketing_log`에서 감사 로그의 `old_value`에 `MarketingLog` 객체를 직렬화할 때, Enum 필드(`stage`)가 `_sanitize_for_json()`을 거치지 않으면 `TypeError: Object of type MarketingStage is not JSON serializable` 발생 가능
- **영향**: DELETE 요청 시 500 에러
- **수정안**: `audit_service.record()`가 내부적으로 처리하는지 확인 필요. 처리하지 않으면 `.value` 명시적 변환 추가

#### [R6-C1] CIM_SENT 레이블 불일치 "IM 발송" vs "CIM 발송" — [Critical/MEDIUM] — Priority: P1 (60)

- **파일**: `amic-platform/src/modules/ma/constants/buyer.ts`
- **카테고리**: R6 비즈니스 & UX (도메인 용어 일관성)
- **검증**: Read(V) Grep(V) SC-1(V) SC-3(V) SC-5(V)
- **이슈**: 동일 enum `CIM_SENT`에 대해:
  - `MARKETING_STAGE_LABELS` (L77): "IM 발송"
  - `BUYER_STATUS_OPTIONS` (L125 부근): "CIM 발송"
- **영향**: 사용자 혼란 — 같은 단계가 UI 위치에 따라 다른 이름으로 표시
- **수정안**: 하나로 통일 (비즈니스 담당자 확인 후 "IM 발송" 또는 "CIM 발송" 선택)

---

### P2 — 개선 권장 (점수: 30-59)

#### [R3-W1] downgrade()가 빈 pass — [Major/MEDIUM] — Priority: P2 (42)

- **파일**: `deal-mgmt/migrations/versions/078_add_marketing_stage_values.py`
- **카테고리**: R5 배포 안전성
- **검증**: Read(V) Grep(V) SC-1(V) SC-3(△) SC-5(V)
- **이슈**: `downgrade()`가 `pass`만 있음. PostgreSQL enum에서 값 제거는 불가하므로 기술적으로는 맞지만, `raise CommandError("Cannot remove enum values")`로 명시적 실패가 더 안전
- **영향**: 롤백 시 "성공했다"고 착각할 수 있음 (실제로 enum 값은 남아 있음)

#### [R3-W2] _ADVANCE_PATH가 _BUYER_STATUS_TRANSITIONS와 불일치 — [Major/MEDIUM] — Priority: P2 (42)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py` vs `deal-mgmt/app/routers/buyers.py`
- **카테고리**: R3 데이터 정합성
- **검증**: Read(V) Grep(V) SC-1(V) SC-3(V) SC-5(△)
- **이슈**: `buyer_marketing.py`의 `_ADVANCE_PATH`에서 `CONTACTED → NDA_SIGNED` (NDA_SENT 건너뜀)이지만, `buyers.py`의 수동 전이 규칙에서는 `CONTACTED → NDA_SENT → NDA_SIGNED`가 유효 경로
- **영향**: 자동 승격과 수동 변경의 상태 전이 경로가 다름. 의도적 설계일 가능성 있으나 확인 필요

#### [R2-M1] DELETE 마케팅 로그 시 buyer.status 롤백 없음 — [Moderate/HIGH] — Priority: P2 (40)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py`
- **카테고리**: R2 보안 / R4 복원력
- **검증**: Read(V) Grep(V) SC-1(V) SC-3(V) SC-5(V)
- **이슈**: 마케팅 로그 삭제 시 자동 승격된 buyer.status를 롤백하지 않음. 플랜에서 의도적 결정으로 명시되어 있으나, 데이터 정합성 관점에서 리스크
- **영향**: NDA_SIGNED 로그 삭제 후에도 buyer.status는 NDA_SIGNED로 유지

#### [R3-W3] PATCH 감사 로그 old_value/new_value 검증 부재 — [Moderate/MEDIUM] — Priority: P2 (36)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py`
- **카테고리**: R3 데이터 정합성
- **검증**: Read(V) Grep(V) SC-1(V) SC-3(△) SC-5(V)
- **이슈**: `update_marketing_log`에서 감사 로그의 old_value/new_value가 실제 변경 필드만 포함하는지, 전체 객체를 직렬화하는지 불명확
- **영향**: 감사 로그 크기 증가, 추적 어려움

---

### P3 — 저우선 (점수: <30)

#### [R6-I1] 비즈니스 로직이 라우터 레이어에 위치 — [Minor/HIGH] — Priority: P3 (20)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py`
- **카테고리**: R6 비즈니스 & UX (계층 배치)
- **이슈**: `_auto_advance_buyer_status()`, `_ADVANCE_PATH`, `_STATUS_ORDER` 등 비즈니스 로직이 라우터 파일에 직접 위치. `services/` 레이어로 분리하면 테스트 용이성과 재사용성 향상
- **영향**: 코드 조직 — 기능적 문제 없음

#### [R6-I2] PHONE_CALL/ADVISOR_MEETING이 buyer.status 승격 트리거 없음 — [Minor/MEDIUM] — Priority: P3 (12)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py`
- **카테고리**: R6 도메인 로직
- **이슈**: `PHONE_CALL`이나 `ADVISOR_MEETING` 마케팅 스테이지가 `_MARKETING_STATUS_ADVANCE`에 없어서 buyer.status를 CONTACTED로 승격하지 않음. 전화 접촉 = CONTACTED로 볼 수 있음
- **영향**: 비즈니스 결정 사항 — 의도적 설계일 수 있음

#### [R6-I3] 타임라인 CircularProgress SVG에 aria 속성 부재 — [Minor/MEDIUM] — Priority: P3 (12)

- **파일**: `amic-platform/src/modules/ma/components/buyers/MarketingTimelineView.tsx`
- **카테고리**: R6 접근성
- **이슈**: 진행률 표시 시각 요소에 `role="progressbar"` 및 `aria-valuenow` 누락
- **영향**: 스크린 리더 사용자 접근성

#### [R6-I4] _auto_advance_buyer_status 단위 테스트 부재 — [Minor/MEDIUM] — Priority: P3 (12)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py`
- **카테고리**: R6 테스트 품질
- **이슈**: 멀티홉 상태 전이, 터미널 상태 가드, 순환 방지 등 복잡한 로직에 단위 테스트가 없음
- **영향**: 향후 상태 추가/변경 시 회귀 위험

#### [R2-M2] dart-search/export-excel에 Rate Limiting 미적용 — [Minor/MEDIUM] — Priority: P3 (12)

- **파일**: `deal-mgmt/app/routers/buyer_marketing.py`
- **카테고리**: R2 보안 (DoS)
- **이슈**: 이번 커밋 범위는 아니지만, 동일 라우터 내 dart-search, export-excel 엔드포인트에 rate limiting 없음
- **영향**: 대량 요청 시 서버 부하

#### [R1-S1] docstring "6단계" → "8단계" 불일치 — [Minor/HIGH] — Priority: P3 (20)

- **파일**: `deal-mgmt/app/schemas/marketing_log.py` (L41, L300)
- **카테고리**: R1 정합성
- **이슈**: 마케팅 스테이지가 8개로 확장되었으나 docstring에 "6단계"로 남아 있음
- **영향**: 개발자 혼란 (기능적 영향 없음)

---

## Priority Matrix

### P0 — 즉시 수정 (0건)
없음

### P1 — 스프린트 우선 (2건)
1. [R3-C1] [Critical/MEDIUM]: DELETE 감사 로그 Enum 직렬화 리스크 — buyer_marketing.py (60)
2. [R6-C1] [Critical/MEDIUM]: CIM_SENT 레이블 불일치 — buyer.ts (60)

### P2 — 개선 권장 (4건)
1. [R3-W1] [Major/MEDIUM]: downgrade() 빈 pass — 078_migration.py (42)
2. [R3-W2] [Major/MEDIUM]: _ADVANCE_PATH vs _BUYER_STATUS_TRANSITIONS 불일치 — buyer_marketing.py (42)
3. [R2-M1] [Moderate/HIGH]: DELETE 시 status 롤백 없음 — buyer_marketing.py (40)
4. [R3-W3] [Moderate/MEDIUM]: PATCH 감사 로그 검증 부재 — buyer_marketing.py (36)

### P3 — 저우선 (6건)
1. [R1-S1] [Minor/HIGH]: docstring "6단계" 불일치 — marketing_log.py (20)
2. [R6-I1] [Minor/HIGH]: 비즈니스 로직 라우터 레이어 배치 — buyer_marketing.py (20)
3. [R6-I2] [Minor/MEDIUM]: PHONE_CALL 승격 매핑 없음 — buyer_marketing.py (12)
4. [R6-I3] [Minor/MEDIUM]: CircularProgress aria 속성 부재 — TimelineView.tsx (12)
5. [R6-I4] [Minor/MEDIUM]: _auto_advance 단위 테스트 부재 — buyer_marketing.py (12)
6. [R2-M2] [Minor/MEDIUM]: Rate Limiting 미적용 — buyer_marketing.py (12)

---

## 검증 투명성

- 검증한 가설: 24건
- 거부된 가설 (사전 제거): 12건
- 보고된 이슈: 12건
- 거부율: 50%

### 거부 사유 분류

| 사유 | 건수 |
|------|------|
| SC-1 실행 경로 기각 | 2 |
| SC-3 라이브러리/프레임워크 오해 기각 | 3 |
| SC-5 의도된 패턴 기각 | 4 |
| Grep 반증 | 2 |
| 범위 외 (이번 커밋과 무관) | 1 |

---

## 기존 리뷰(20260310_2233)와의 교차 비교

| 기존 리뷰 이슈 | 이번 리뷰 확인 |
|---------------|-------------|
| [BE-1] _TERMINAL_STATUSES 데드 코드 | R3-W2에서 재확인 (동일 근본 원인) |
| [SEC-1] Race Condition | 이번 리뷰에서도 인지하나 별도 보고 안 함 (기존 리뷰에서 이미 P1) |
| [BE-2] NDA_SENT 건너뜀 | R3-W2에 통합 |
| [FE-1] 레이블 불일치 | R6-C1으로 재확인 (동일) |

### 신규 발견 (기존 리뷰에 없던 이슈)

- [R3-C1] DELETE 감사 로그 Enum 직렬화 리스크 — **신규**
- [R3-W1] downgrade() CommandError 권장 — **신규**
- [R2-M1] DELETE 시 status 롤백 없음 — **신규** (플랜에서 의도적 결정이나 리스크 명시)
- [R6-I1~I4] 계층 배치, PHONE_CALL 매핑, aria 속성, 단위 테스트 — **모두 신규**
- [R1-S1] docstring "6단계" — **신규**

---

## 다음 라운드

- 다음 실행: R1 (기본 체크리스트) — 순환 리셋
