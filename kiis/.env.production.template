# =============================================================================
# KIIS - Production Environment Configuration
# =============================================================================
# IMPORTANT: This is a TEMPLATE for production deployments.
# 1. Copy this file to .env
# 2. Replace ALL placeholder values marked with "CHANGE-THIS"
# 3. Never commit the actual .env file to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_NAME=KIIS
DEBUG=false

# CRITICAL: Generate a strong secret key before deploying!
# Run: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE-THIS-generate-with-secrets-token-urlsafe-64

# -----------------------------------------------------------------------------
# Database (PostgreSQL) - Uses Docker service name as host
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql+asyncpg://kiis_user:CHANGE-THIS-strong-db-password@postgres:5432/kiis
POSTGRES_PASSWORD=CHANGE-THIS-strong-db-password

# -----------------------------------------------------------------------------
# Redis - Uses Docker service name as host
# -----------------------------------------------------------------------------
REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# ElasticSearch - Uses Docker service name as host
# -----------------------------------------------------------------------------
ELASTICSEARCH_URL=http://elasticsearch:9200

# -----------------------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------------------
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------------------------------------------------------
# DART API (금융감독원 전자공시)
# -----------------------------------------------------------------------------
DART_API_KEY=CHANGE-THIS-your-production-dart-api-key
DART_BASE_URL=https://opendart.fss.or.kr/api
DART_RATE_LIMIT_PER_MINUTE=900
DART_RATE_LIMIT_PER_DAY=9000

# -----------------------------------------------------------------------------
# KOFIA (금융투자협회)
# -----------------------------------------------------------------------------
KOFIA_DIS_BASE_URL=https://dis.kofia.or.kr
KOFIA_RATE_LIMIT_PER_MINUTE=20
KOFIA_RATE_LIMIT_PER_DAY=1000
KOFIA_REQUEST_DELAY=3.0

# -----------------------------------------------------------------------------
# REITs (리츠정보시스템)
# -----------------------------------------------------------------------------
REITS_BASE_URL=https://reits.molit.go.kr
REITS_RATE_LIMIT_PER_MINUTE=20
REITS_RATE_LIMIT_PER_DAY=500
REITS_REQUEST_DELAY=3.0

# -----------------------------------------------------------------------------
# Docker / Uvicorn
# -----------------------------------------------------------------------------
WORKERS=4
